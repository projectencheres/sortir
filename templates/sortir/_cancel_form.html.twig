{# <div class="modal fade" id="cancelModal" tabindex="-1" aria-labelledby="cancelModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header bg-danger text-white">
                <h5 class="modal-title" id="cancelModalLabel">
                    Annuler la sortie : {{ sortie.nom }}
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p class="text-muted mb-3">
                    Veuillez fournir un motif d'annulation pour la sortie <strong>{{ sortie.nom }}</strong>.
                </p>
                {{ form_start(form, { 'attr': { 'id': 'cancel-form' } }) }}
                    <div class="mb-3">
                        {{ form_label(form.motif, 'Motif d\'annulation', { 'label_attr': { 'class': 'form-label fw-bold' } }) }}
                        {{ form_widget(form.motif, { 'attr': { 'class': 'form-control', 'rows': 3, 'placeholder': 'Exemple : Problème météorologique' } }) }}
                        {{ form_errors(form.motif) }}
                    </div>
                    <div class="d-flex justify-content-end">
                        <button type="button" class="btn btn-secondary me-2" data-bs-dismiss="modal">Annuler</button>
                        <button type="submit" class="btn btn-danger">Confirmer l'annulation</button>
                    </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
</div> #}

<form id="cancel-form" action="{{ path('app_sortir_cancel', {id: sortie.id}) }}" method="post">
    {{ form_row(form.motif) }}
    <button type="submit" class="btn btn-danger">
        Confirmer l'annulation
    </button>
</form>

